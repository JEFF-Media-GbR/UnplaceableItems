main: ${spigot.main}
name: ${project.name}
version: ${project.version}
prefix: ${spigot.prefix}
website: "${spigot.website}"
author: mfnalex
database: false
api-version: "1.13"
load: POSTWORLD
commands:
  nbt: